{
  "name": "revstack-os",
  "private": false,
  "license": "SEE LICENSE IN LICENSE.md",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "smoke": "node scripts/smoke-cli.js",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "publish-packages": "turbo run build && changeset publish",
    "release:dev": "changeset add --empty && changeset version --snapshot dev && pnpm -r exec npm version 0.0.0 --no-git-tag-version && turbo build && changeset publish --no-git-tag --tag dev"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@types/node": "^20.11.30",
    "dotenv": "^17.2.4",
    "prettier": "^3.7.4",
    "tsx": "^4.21.0",
    "turbo": "^2.8.1",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "stripe": "^20.3.1"
  }
}
