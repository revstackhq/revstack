import { WebhookHandler } from "@revstackhq/providers-core";

export const handleSubscriptionActive: WebhookHandler = async (raw, _ctx) => {
  // TODO: Implement handleSubscriptionActive mapped event extraction
  return Promise.resolve(null);
};
