import { WebhookHandler } from "@revstackhq/providers-core";

export const handleOrderCreated: WebhookHandler = async (raw, _ctx) => {
  // TODO: Implement handleOrderCreated mapped event extraction
  return Promise.resolve(null);
};
