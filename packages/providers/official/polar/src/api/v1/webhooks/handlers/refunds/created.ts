import { WebhookHandler } from "@revstackhq/providers-core";

export const handleRefundCreated: WebhookHandler = async (raw, _ctx) => {
  // TODO: Implement handleRefundCreated mapped event extraction
  return Promise.resolve(null);
};
