import { WebhookHandler } from "@revstackhq/providers-core";

export const handleSubscriptionCreated: WebhookHandler = async (raw, _ctx) => {
  // TODO: Implement handleSubscriptionCreated mapped event extraction
  return Promise.resolve(null);
};
