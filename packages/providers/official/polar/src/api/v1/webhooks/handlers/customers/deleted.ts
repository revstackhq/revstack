import { WebhookHandler } from "@revstackhq/providers-core";

export const handleCustomerDeleted: WebhookHandler = async (raw, _ctx) => {
  // TODO: Implement handleCustomerDeleted mapped event extraction
  return Promise.resolve(null);
};
