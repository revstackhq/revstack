import { WebhookHandler } from "@revstackhq/providers-core";

export const handleCustomerStateChanged: WebhookHandler = async (raw, _ctx) => {
  // TODO: Implement handleCustomerStateChanged mapped event extraction
  return Promise.resolve(null);
};
