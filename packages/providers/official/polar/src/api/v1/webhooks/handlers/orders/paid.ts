import { WebhookHandler } from "@revstackhq/providers-core";

export const handleOrderPaid: WebhookHandler = async (raw, _ctx) => {
  // TODO: Implement handleOrderPaid mapped event extraction
  return Promise.resolve(null);
};
