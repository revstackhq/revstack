import { WebhookHandler } from "@revstackhq/providers-core";

export const handleSubscriptionPastDue: WebhookHandler = async (raw, _ctx) => {
  // TODO: Implement handleSubscriptionPastDue mapped event extraction
  return Promise.resolve(null);
};
