import { WebhookHandler } from "@revstackhq/providers-core";

export const handleRefundUpdated: WebhookHandler = async (raw, _ctx) => {
  // TODO: Implement handleRefundUpdated mapped event extraction
  return Promise.resolve(null);
};
