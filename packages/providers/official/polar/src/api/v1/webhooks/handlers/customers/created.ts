import { WebhookHandler } from "@revstackhq/providers-core";

export const handleCustomerCreated: WebhookHandler = async (raw, _ctx) => {
  // TODO: Implement handleCustomerCreated mapped event extraction
  return Promise.resolve(null);
};
