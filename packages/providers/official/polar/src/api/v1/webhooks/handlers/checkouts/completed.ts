import { WebhookHandler } from "@revstackhq/providers-core";

export const handleCheckoutCompleted: WebhookHandler = async (raw, _ctx) => {
  // TODO: Implement handleCheckoutCompleted mapped event extraction
  return Promise.resolve(null);
};
