import { WebhookHandler } from "@revstackhq/providers-core";

export const handleOrderRefunded: WebhookHandler = async (raw, _ctx) => {
  // TODO: Implement handleOrderRefunded mapped event extraction
  return Promise.resolve(null);
};
