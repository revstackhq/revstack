import { WebhookHandler } from "@revstackhq/providers-core";

export const handleSubscriptionCanceled: WebhookHandler = async (raw, _ctx) => {
  // TODO: Implement handleSubscriptionCanceled mapped event extraction
  return Promise.resolve(null);
};
