import { WebhookHandler } from "@revstackhq/providers-core";

export const handleOrderUpdated: WebhookHandler = async (raw, _ctx) => {
  // TODO: Implement handleOrderUpdated mapped event extraction
  return Promise.resolve(null);
};
